<div class="container">
    <button class="clear-all">Clear All Articles</button>



{{#each articles}}
    <div class="card text-center">
    <div class="card-header">
        <ul class="nav nav-pills card-header-pills">
        <li class="nav-item">
            <a class="nav-link active" href="{{url}}">Link to Article</a>
        </li>
        </ul>
    </div>
    <div class="card-body">
        <h5 class="card-title headline">{{headline}}</h5>
        <p class="card-text summary">{{summary}}</p>
        <button type="button" class="btn btn-outline-info add-note note-button" data-id="{{_id}}">Add/View Notes</button>
        <button type="button" class="btn btn-outline-danger delete-art" data-id={{_id}}>Delete</button>
        <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#exampleModalCenter">
        Launch demo modal
        </button>
    </div>
    </div>
   
{{/each}}

    <div class="noteModal" id="noteModal" tabindex="-1">
    <div class="">
        <div class="modal-content">
        <div class="modal-header">
            <h5 class="modal-title" id="exampleModalLongTitle">Modal title</h5>
            {{!-- <button type="button" class="close" data-dismiss="modal" aria-label="Close"> --}}
            {{!-- </button> --}}
            Note Modal 
        </div>
        <div class="modal-body">
            <div>
                Notes
            </div>
            <div>
                <form>
                    <textarea id="noteContent" placeholder="Add note"></textarea>
                </form>
            </div>
        </div>
        <div class="modal-footer">
            <button type="button" class="btn btn-secondary closeButton">Close</button>
            <button type="button" class="btn btn-primary saveNoteButton">Post Note</button>
        </div>
        </div>
    </div>
    </div>

    {{!-- <div class="modal fade" id="exampleModalCenter" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
        <div class="modal-header">
            <h5 class="modal-title" id="exampleModalLongTitle">Modal title</h5>
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
            </button>
        </div>
        <div class="modal-body">
            ...
        </div>
        <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
            <button type="button" class="btn btn-primary">Save changes</button>
        </div>
        </div>
    </div>
    </div> --}}

    {{!-- <div class="modal fade" id="note-modal-{{_id}}" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered" role="document">
            <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Notes Pinned to {{headline}}</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                ...
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                <button type="button" class="btn btn-primary">Save changes</button>
            </div>
            </div>
        </div>
    </div> --}}


</div>
<!-- Modal -->


{{!-- <div id="note-modal">
    <div class="modal-dialog modal-body">
        <button class="modal-close-default" type="button" ></button>
        <h2 class="uk-modal-title modal-title">Article Notes For:</h2>
        <h6 class="uk-modal-title note-title"></h6>
        <hr>
        <div class="saved-notes">
            <p class="no-notes">No notes for this article yet.</p>
        </div>
        <form>
            <textarea class="uk-textarea new-note note-content" placeholder="New Note"></textarea>
        </form>
        <p class="uk-text-right">
            <button class="save-note uk-button uk-button-primary uk-modal-close" type="button">Save</button>
        </p>
    </div>
</div>

{{#each article}} 
  {{> note-modal }} 
{{/each}} --}}
